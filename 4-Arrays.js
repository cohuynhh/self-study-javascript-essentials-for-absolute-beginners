const names = ["Alan", "Nathan", "Lele", "Kim", "Aladin"];

console.log(names);

names[1];
names[1 + 1];
names[100]; //undefined

const arrNames = [];

arrNames.push("Nathan");
arrNames.pop(); //remove last element in array

arrNames[0] = undefined;

arrNames.length();

const name = "Nathan Huynh";
name[0]; //"N"
name[1]; //"a"

const nameArray = Array.from(name);




