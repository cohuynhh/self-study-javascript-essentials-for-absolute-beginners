for (let i = 0; i <= 10; i++){
    console.log(i);
}

const names = ["xbbg", "ya", "zerer"];
for(let i = 0; i < names.length; i++) {
    if(name[1].length > 3) {
        console.log(name[i]);
        console.log(`Found ok item at index ${i}`);
        break;
    }       
}

names.forEach(e => console.log(e));